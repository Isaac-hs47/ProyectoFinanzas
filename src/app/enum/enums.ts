export enum BadgeTypes{
    Bronze,
    Silver,
    Gold
}

export enum BadgesCodes{
    NEW_BEGINNING,
    FIRST_HIT,
    BEGINNER,
    INTERMEDIATE,
    EXPERT,
    FIRST_TEST
}

export enum StorageVariables{
    USERS = "Users",
    SESSION = "Session"
}

export enum ExamLevels{
  BASIC,
  INTERMEDIATE,
  ADVANCE
}
